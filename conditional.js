// Condition
 
/*
    ===     => equal
    >=      => greater then or equal
    <=      => less then or equal
    !==     => not equal
    <       => less then
    >       => greater then
    !       => not
    &&      => and both are true
    ||      => any one need to true

*/

if (a === b) {
    console.log('A and B is equal.')
}
else {
    console.log('A and B is not equal.')
}